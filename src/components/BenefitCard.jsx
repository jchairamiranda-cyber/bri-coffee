/**
 * @deprecated — Logic absorbed into the new section components
 * (BrandStory pillars row, Ingredients cards). Not imported anywhere;
 * safe to delete manually.
 */
export default function BenefitCard() {
  return null;
}
