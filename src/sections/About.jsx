/**
 * @deprecated — Replaced by `BrandStory.jsx`.
 *
 * This file is no longer imported by App.jsx and Vite will not bundle it.
 * You can safely delete it manually. Kept only because the local sandbox
 * cannot remove files in the workspace folder.
 */
export default function About() {
  return null;
}
